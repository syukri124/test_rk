name: test_rk
description: A new Flutter project for RK Café POS App
publish_to: 'none' # prevent accidental publishing to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Icon bawaan gaya iOS (opsional)
  cupertino_icons: ^1.0.8

  # Tambahkan dependency lain di sini kalau dibutuhkan nanti
  # contoh:
  # provider: ^6.1.2
  # http: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
