import 'package:flutter/material.dart';

class TambahPesananScreen extends StatelessWidget {
  const TambahPesananScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Tambah Pesanan')),
      body: const Center(child: Text('Halaman Tambah Pesanan')),
    );
  }
}
